<%= render 'shared/sort' %>
<% if @notes.any? %>
  <% cache @notes do %>
    <ul id="notes" data-update-url="<%= sort_notes_url %>" class="notes">
      <%# <%= render partial: 'shared/note', collection: @notes %>
      <%= render @notes %>
    </ul>
  <%end%>
<% end %>
