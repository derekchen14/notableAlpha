<% if @notes.any? %>
  <ul id="notes" data-update-url="<%= sort_notes_url %>" class="notes">
    <%= render partial: 'shared/note', collection: @notes.order("position") %>
  </ul>
  <%= will_paginate @notes %>
<% end %>
