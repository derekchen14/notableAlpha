<li>
	    <!--Add a checkbox here-->
  <span class="content"><%= note.content %></span>
  <span class="timestamp">
    Posddddted <%= time_ago_in_words(note.created_at) %> ago.
  </span>

  <% if current_user?(note.user) %>
  <%= link_to "delete", note, method: :delete,
  	data: { confirm: "You sure?" }, title: note.content %>
  <% end %>

  <%= link_to "Set Reminder", '#', class: "btn btn-large pull-right add_phone" %>
</li>