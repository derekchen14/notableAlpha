<%= form_for @note, method: :put do |f| %>
  <%= f.text_field :tag_list, id: "tag_list_#{@note.id}", data:{load: @note.tags} %>

  <%= f.submit "Add Tag", class: "btn btn-small btn-primary phone_submit" %>
<% end %>
