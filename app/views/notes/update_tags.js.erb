$("#note_<%= @note.id %>_tags").empty();
$("#tags_<%= @note.id %>").html("<%= raw @note.tags.map(&:name).map { |t| j(link_to t, filter_tag_path(t), remote: true) }.join(', ') %>");
$("#tags_<%= @note.id %>").show();

$("#sidr").empty();
$("#sidr").html("<%= j render "features/taglist", collection: @tags%>");
