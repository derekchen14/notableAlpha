
  <%= form_for @tag do |f| %>
    <%= f.text_field :name %>
    <%# <%= f.text_field :tag_list, {load: @note.tags} %>

    <%= f.submit "Add Tag", class: "btn btn-small btn-primary phone_submit" %>
  <% end %>

